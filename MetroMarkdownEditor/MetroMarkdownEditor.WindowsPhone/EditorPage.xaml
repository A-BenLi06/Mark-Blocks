<Page
    x:Class="MetroMarkdownEditor.WindowsPhone.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Editor, Source={StaticResource Locator}}">

    <Page.Resources>
        <Style x:Key="AppBarLikeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{ThemeResource ApplicationForegroundThemeBrush}" />
        </Style>

        <Style x:Key="FileTabItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="SelectionStates">
                                    <VisualState x:Name="Unselected" />
                                    <VisualState x:Name="Selected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ColorContainer" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <SolidColorBrush Color="{ThemeResource SystemAccentColor}" />
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ColorContainer" Storyboard.TargetProperty="FontWeight">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <FontWeight>Medium</FontWeight>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <ContentControl x:Name="ColorContainer"
                                            Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Center">
                                <ContentPresenter x:Name="TabContent"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Margin="8,4,8,4" />
                            </ContentControl>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Immersive editor surface: no borders, no focus color changes -->
        <Style x:Key="ImmersiveRichEditBoxStyle" TargetType="RichEditBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RichEditBox">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver"/>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement"
                                                                           Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                                    Value="{ThemeResource TextControlDisabledForeground}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>

                                <VisualStateGroup x:Name="FocusStates">
                                    <VisualState x:Name="Focused"/>
                                    <VisualState x:Name="Unfocused"/>
                                    <VisualState x:Name="PointerFocused"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <ScrollViewer x:Name="ContentElement"
                                          Padding="{TemplateBinding Padding}"
                                          Background="Transparent"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          IsTabStop="False"
                                          ZoomMode="Disabled" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Undo" Click="UndoButton_Click">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Undo"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <AppBarButton Label="Redo" Click="RedoButton_Click">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Redo"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <AppBarButton Label="Mode" Command="{Binding ToggleViewModeCommand}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Refresh"/>
                    </AppBarButton.Icon>
                </AppBarButton>
            </CommandBar.PrimaryCommands>

            <CommandBar.SecondaryCommands>
                <AppBarButton Label="New" Command="{Binding NewCommand}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Add"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <AppBarButton Label="Open" Command="{Binding OpenCommand}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Folder"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <AppBarButton Label="Save" Command="{Binding SaveCommand}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Save"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <AppBarButton Label="Export">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="Segoe UI Symbol" Glyph="&#xE11C;" />
                    </AppBarButton.Icon>
                    <AppBarButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="To Markdown" Tag="md" Click="ExportMenuItem_Click" />
                            <MenuFlyoutItem Text="To HTML" Tag="html" Click="ExportMenuItem_Click" />
                            <MenuFlyoutItem Text="To PDF" Tag="pdf" Click="ExportMenuItem_Click" />
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>

                <AppBarButton Label="Auto Save" Click="AutoSaveSettings_Click">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Setting"/>
                    </AppBarButton.Icon>
                </AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                BorderBrush="{ThemeResource ApplicationForegroundThemeBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,8,12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button x:Name="BackButton"
                        Content="&#xE0A6;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="24"
                        Style="{StaticResource AppBarLikeButtonStyle}"
                        Click="BackButton_Click"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center" />

                <ListView Grid.Column="1"
                          ItemsSource="{Binding OpenDocuments}"
                          SelectedItem="{Binding ActiveDocument, Mode=TwoWay}"
                          SelectionMode="Single"
                          ScrollViewer.HorizontalScrollMode="Enabled"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollMode="Disabled"
                          Margin="12,0,0,0"
                          ItemContainerStyle="{StaticResource FileTabItemStyle}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock x:Name="TitleText"
                                           Grid.Column="0"
                                           Text="{Binding Title}"
                                           FontSize="18"
                                           VerticalAlignment="Center" />

                                <Button Grid.Column="1"
                                        Width="28" Height="28"
                                        Margin="8,0,0,0"
                                        Command="{Binding DataContext.CloseDocumentCommand, ElementName=LayoutRoot}"
                                        CommandParameter="{Binding}">
                                    <Button.Foreground>
                                        <Binding ElementName="TitleText" Path="Foreground" />
                                    </Button.Foreground>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid Background="Transparent">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <TextBlock Text="&#xE106;"
                                               FontFamily="Segoe UI Symbol"
                                               FontSize="14" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                    Visibility="{Binding ViewMode, Converter={StaticResource ViewModeToVisibilityConverter}, ConverterParameter=Editor}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="36" />
                    </Grid.RowDefinitions>

                    <RichEditBox x:Name="EditorBox"
                                 Grid.Row="0"
                                 Style="{StaticResource ImmersiveRichEditBoxStyle}"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 IsSpellCheckEnabled="False"
                                 FontFamily="Consolas"
                                 FontSize="21"
                                 BorderThickness="0"
                                 Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                 Padding="18,14,18,14"
                                 Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                                 TextChanged="EditorBox_TextChanged" 
                                 KeyDown="EditorBox_KeyDown"/>

                    <!-- Minimal safe area to help summon app bar without shifting the editor -->
                    <Border Grid.Row="1"
                            Background="Transparent"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" />
                </Grid>
            </Border>

            <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                    Visibility="{Binding ViewMode, Converter={StaticResource ViewModeToVisibilityConverter}, ConverterParameter=Preview}">
                <WebView x:Name="PreviewWebView"
                         NavigationCompleted="PreviewWebView_NavigationCompleted" />
            </Border>
        </Grid>
    </Grid>
</Page>
